# 更新日志

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

---

## [v0.3.0] - 2026-05-09

### 新增
- 操作区新增**「多页合并为 PDF」**开关（默认关闭）
  - 开启后，处理多页 PDF 文件时将所有带水印的页面合并输出为单个 PDF，而非逐页拆分为多张 PNG
  - 每页可独立宽高，横竖混排文档均可正确处理
  - 单页文件（图片或单页 PDF）不受影响，仍输出 PNG

### 变更
- CI 触发条件调整：仅在推送 `v*` 标签时自动构建，推送 master 分支不再触发（避免每次提交都占用 CI 资源）

---

## [v0.2.0] - 2026-05-09

### 新增
- 文件列表顶部增加**全选 / 取消全选**三态复选框，显示当前筛选范围内已选数量（如 `全选（3/10）`）
- 点击复选框或行文字均可触发全选；部分选中时显示半选状态
- `store` 新增 `selectAll()` / `clearSelection()` 方法，仅作用于当前可见文件，不影响隐藏文件的选中状态

---

## [v0.1.0] - 2026-05-09

### 新增（初始版本）

**核心功能**
- Tauri 2 + Vue 3 + TypeScript 桌面应用，支持 macOS（universal）和 Windows
- 选择本地目录后自动扫描并索引所有图片（jpg/jpeg/png/webp/bmp/tiff）与 PDF 文件
- 左侧扁平文件列表，支持方向键 ↑ / ↓ 快速切换预览
- 右侧单文件预览（图片直接渲染；PDF 分页预览并可翻页）

**标签管理**
- 为每个文件打标签，标签存入本地 IndexedDB
- 支持按标签多选筛选、文件名模糊搜索

**水印功能**
- 实时预览水印效果（对齐 Canvas 绘制逻辑）
- 水印文字、透明度、颜色、旋转角度可调
- 字号以**相对短边比例**表示，大小图视觉一致
- 单点模式（九宫格定位）与**全屏平铺**模式
- 全屏平铺支持**横向间距**与**纵向间距**独立调节（纵向范围 1–5×，默认 2×）
- 全屏平铺支持**交错**（砖缝错位）效果
- 水印设置本地持久化（app-store）

**批量导出**
- 勾选多个文件后一键生成带水印版本
- 所有文件打包为 ZIP 下载，文件名保留原始名称并标记页码


### 新增
- 文件列表顶部增加**全选 / 取消全选**三态复选框，显示当前筛选范围内已选数量（如 `全选（3/10）`）
- 点击复选框或行文字均可触发全选；部分选中时显示半选状态
- `store` 新增 `selectAll()` / `clearSelection()` 方法，仅作用于当前可见文件，不影响隐藏文件的选中状态

---

## [v0.1.0] - 2026-05-09

### 新增（初始版本）

**核心功能**
- Tauri 2 + Vue 3 + TypeScript 桌面应用，支持 macOS（universal）和 Windows
- 选择本地目录后自动扫描并索引所有图片（jpg/jpeg/png/webp/bmp/tiff）与 PDF 文件
- 左侧扁平文件列表，支持方向键 ↑ / ↓ 快速切换预览
- 右侧单文件预览（图片直接渲染；PDF 分页预览并可翻页）

**标签管理**
- 为每个文件打标签，标签存入本地 IndexedDB
- 支持按标签多选筛选、文件名模糊搜索

**水印功能**
- 实时预览水印效果（对齐 Canvas 绘制逻辑）
- 水印文字、透明度、颜色、旋转角度可调
- 字号以**相对短边比例**表示，大小图视觉一致
- 单点模式（九宫格定位）与**全屏平铺**模式
- 全屏平铺支持**横向间距**与**纵向间距**独立调节（纵向范围 1–5×，默认 2×）
- 全屏平铺支持**交错**（砖缝错位）效果
- 水印设置本地持久化（app-store）

**批量导出**
- 勾选多个文件后一键生成带水印版本
- 所有文件打包为 ZIP 下载，文件名保留原始名称并标记页码

